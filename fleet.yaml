#
# Next iteration: Claim the Trident installation

defaultNamespace: trident-csi
helm:
  releaseName: trident
  repo: https://artifacts.solvinity.net/repository/helm3-sse-acc/
  chart: trident-operator
  version: 22.10.0
  values:
    kubeletDir: /opt/rke/var/lib/kubelet
    imageRegistry: harbor.solvinity.net/picard
kustomize:
  dir: overlays
